package Chatbot;

// import

public class Network{
    public static double[] LSTM(double Xt, double lCt, double lht) {
        double sigmoid(double x) {
            return 1 / (1 + Math.exp(x));
        }

        double tanh(double x) {
            return Math.tanh(x);
        }

        return [1,1,1];
    }
}